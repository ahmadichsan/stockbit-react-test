/// Name the interface with I prefix: e.g INameHere {}

import { TooltipProps } from "react-tooltip";

export declare interface ReactTooltipWrapperProps extends TooltipProps {
  textAlign?: "right" | "left" | "center";
}
